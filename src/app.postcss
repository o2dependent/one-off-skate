/* Write your global styles here, in PostCSS syntax */
@tailwind base;
@tailwind components;
@tailwind utilities;

html,
body {
	@apply h-full w-full bg-base-300;
}

.tab {
	--tab-bg: hsl(var(--b1, var(--b2)) / var(--tw-border-opacity));
}
